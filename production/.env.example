# QuestCord Environment Configuration Example
# Copy this file to .env and fill in your actual values

# ===========================================
# DISCORD BOT CONFIGURATION
# ===========================================
# Get these from https://discord.com/developers/applications
DISCORD_TOKEN=your_discord_bot_token_here

# IMPORTANT: Replace 'your_discord_application_client_id' with your ACTUAL Discord Application ID
# This is a NUMERIC value (e.g., 1234567890123456789), NOT the text "your_discord_application_client_id"
# Find it at: https://discord.com/developers/applications > Your App > Application ID
DISCORD_CLIENT_ID=your_discord_application_client_id

DISCORD_CLIENT_SECRET=your_discord_application_client_secret

# Legacy support (use DISCORD_CLIENT_ID instead)
# CLIENT_ID=your_discord_application_client_id

# Discord OAuth Configuration
DISCORD_SCOPE=identify
DISCORD_PROMPT=consent

# ===========================================
# WEB SERVER CONFIGURATION
# ===========================================
# Port for the web server (optional, defaults to config.json)
PORT=3001

# Public base URL for external access (your domain)
# This overrides the config.json setting if provided
PUBLIC_BASE_URL=https://questcord.fun

# Legacy fallback URL (use PUBLIC_BASE_URL instead)
# PUBLIC_URL=https://questcord.fun

# ===========================================
# AUTHENTICATION & SECURITY
# ===========================================
# Secret for OAuth state parameter (generate a random string)
STATE_SECRET=your_random_state_secret_here

# Session secret for cookie encryption (REQUIRED in production)
# Generate a long random string (at least 32 characters)
# Example: openssl rand -base64 32
SESSION_SECRET=your_session_secret_here_minimum_32_characters_long

# ===========================================
# PAYMENT INTEGRATION (PAYPAL)
# ===========================================
# PayPal environment: sandbox or live
PAYPAL_ENV=sandbox
PAYPAL_CLIENT_ID=your_paypal_client_id
PAYPAL_CLIENT_SECRET=your_paypal_client_secret
PAYPAL_WEBHOOK_ID=your_paypal_webhook_id

# ===========================================
# OPTIONAL OVERRIDES
# ===========================================
# Node environment
NODE_ENV=production

# ===========================================
# SECURITY NOTES
# ===========================================
# - NEVER commit the actual .env file to version control
# - Keep your tokens and secrets secure
# - Use different values for development/production
# - Regenerate secrets if they may have been compromised